name: Create release
on:
  workflow_dispatch:
jobs:
  build:
    name: Create release and build
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - run: echo "hello!"